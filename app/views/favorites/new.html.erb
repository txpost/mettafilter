<h1>New favorite</h1>

<% form_for(@favorite) do |f| %>
  <%= f.error_messages %>
  
  <p>
    <%= f.submit 'Click to add to favorites!' %>
  </p>
  
  <%= f.hidden_field :entry_id, :value => params[:entry] %>
  <%= f.hidden_field :user_id, :value => params[:user] %>
  
<% end %>

<%= link_to 'Back', favorites_path %>
